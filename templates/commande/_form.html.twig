{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center mb-5">
        <button class="btn btn-warning">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
